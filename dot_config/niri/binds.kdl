binds {
    // -- Applications shortcuts
    //Mod+D 	hotkey-overlay-title="Run an Application: fuzzel" { spawn "fuzzel"; }
    //Mod+Space   { spawn-sh "qs -c noctalia-shell ipc call launcher toggle"; }
    Mod+Space   { spawn-sh "noctalia msg panel-toggle launcher"; }
    Mod+T 	hotkey-overlay-title="Open a Terminal: kitty"     { spawn "kitty"; }
    Mod+Alt+T 	hotkey-overlay-title="Open configurations"        { spawn-sh "kitty sh -c \"~/.config/niri/scripts/configs_menu.sh\""; }
    //Super+Alt+L hotkey-overlay-title="Lock the Screen: swaylock"  { spawn "swaylock"; }

    Mod+E	{ spawn "nautilus"; }
    Mod+B 	{ spawn "zen-browser"; }
    //Mod+Shift+E { spawn "qs" "-c" "noctalia-shell" "ipc" "call" "sessionMenu" "toggle"; }
    Mod+Shift+E { spawn-sh "noctalia msg panel-toggle session"; }

    // -- Overlay
    Mod+O repeat=false { toggle-overview; }

    // -- Focus
    Mod+V       { toggle-window-floating; }
    Mod+Shift+V { switch-focus-between-floating-and-tiling; }

    // Audio & Brightness
    //Mod+Alt+Prior  { spawn "qs" "-c" "noctalia-shell" "ipc" "call" "volume" "increase"; }
    //Mod+Alt+Next { spawn "qs" "-c" "noctalia-shell" "ipc" "call" "volume" "decrease"; }
    //Mod+Alt+Pause { spawn "qs" "-c" "noctalia-shell" "ipc" "call" "volume" "muteOutput"; }
    //XF86AudioRaiseVolume { spawn "qs" "-c" "noctalia-shell" "ipc" "call" "volume" "increase"; }
    //XF86AudioLowerVolume { spawn "qs" "-c" "noctalia-shell" "ipc" "call" "volume" "decrease"; }
    //XF86AudioMute { spawn "qs" "-c" "noctalia-shell" "ipc" "call" "volume" "muteOutput"; }
    //XF86MonBrightnessUp { spawn "qs" "-c" "noctalia-shell" "ipc" "call" "brightness" "increase"; }
    //XF86MonBrightnessDown { spawn "qs" "-c" "noctalia-shell" "ipc" "call" "brightness" "decrease"; }
    Mod+Alt+Prior  { spawn-sh "noctalia msg volume-up"; }
    Mod+Alt+Next { spawn-sh "noctalia msg volume-down"; }
    Mod+Alt+Pause { spawn-sh "noctalia msg volume-mute"; }
    XF86AudioRaiseVolume { spawn-sh "noctalia msg volume-up"; }
    XF86AudioLowerVolume { spawn-sh "noctalia msg volume-down"; }
    XF86AudioMute { spawn-sh "noctalia msg volume-mute"; }
    XF86MonBrightnessUp { spawn-sh "noctalia msg brightness-up"; }
    XF86MonBrightnessDown { spawn-sh "noctalia msg brightness-down"; }

    // -- Close windows
    Mod+Q  repeat=false { close-window; }
    ALT+F4 repeat=false { close-window; }

    // -- Window Movement and Focus
    Mod+Left             { focus-column-left; }
    Mod+Down             { focus-window-down; }
    Mod+Up               { focus-window-up; }
    Mod+Right            { focus-column-right; }

    Mod+Ctrl+Left        { move-column-left; }
    Mod+Ctrl+Down        { move-window-down; }
    Mod+Ctrl+Up          { move-window-up; }
    Mod+Ctrl+Right       { move-column-right; }

    Mod+Home             { focus-column-first; }
    Mod+End              { focus-column-last; }
    Mod+Ctrl+Home        { move-column-to-first; }
    Mod+Ctrl+End         { move-column-to-last; }

    Mod+Shift+Left       { focus-monitor-left; }
    Mod+Shift+Down       { focus-monitor-down; }
    Mod+Shift+Up         { focus-monitor-up; }
    Mod+Shift+Right      { focus-monitor-right; }

    Mod+Shift+Ctrl+Left  { move-column-to-monitor-left; }
    Mod+Shift+Ctrl+Down  { move-column-to-monitor-down; }
    Mod+Shift+Ctrl+Up    { move-column-to-monitor-up; }
    Mod+Shift+Ctrl+Right { move-column-to-monitor-right; }

    Mod+Page_Down        { focus-workspace-down; }
    Mod+Page_Up          { focus-workspace-up; }
    Mod+Ctrl+Page_Down   { move-column-to-workspace-down; }
    Mod+Ctrl+Page_Up     { move-column-to-workspace-up; }

    Mod+Shift+Page_Down  { move-workspace-down; }
    Mod+Shift+Page_Up    { move-workspace-up; }

    //Mod+Tab              { focus-workspace-previous; }

    Mod+BracketLeft      { consume-or-expel-window-left; }
    Mod+BracketRight     { consume-or-expel-window-right; }

    Mod+Comma            { consume-window-into-column; }
    Mod+Period           { expel-window-from-column; }

    Mod+R                { switch-preset-column-width; }
    Mod+Shift+R          { switch-preset-window-height; }
    Mod+Ctrl+R           { reset-window-height; }

    Mod+C                { center-column; }
    Mod+Ctrl+C           { center-visible-columns; } // Center all fully visible columns on screen.
    
    Mod+W                { toggle-column-tabbed-display; }

    // -- Window size
    Mod+F                 { maximize-column; }
    Mod+Shift+F           { fullscreen-window; }

    Mod+KP_Add            { set-column-width "+10%"; }
    Mod+KP_Subtract       { set-column-width "-10%"; }

    Mod+Shift+KP_Add      { set-window-height "+10%"; }
    Mod+Shift+KP_Subtract { set-window-height "-10%"; }

    // -- Screenshot
    Print      { screenshot; }
    Ctrl+Print { screenshot-window; }
    Alt+Print  { screenshot-screen; }
    //Print      { spawn-sh "noctalia msg screenshot-region"; }
    //Ctrl+Print { spawn-sh "noctalia msg screenshot-fullscreen"; }
    //Alt+Print  { spawn-sh "noctalia msg screenshot-fullscreen pick"; }

    // -- Emergency scape key when a fullscreen app blocks your keybinds.
    Mod+Escape allow-inhibiting=false { toggle-keyboard-shortcuts-inhibit; }
}
